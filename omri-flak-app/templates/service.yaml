service:
  apiVersion: v1
  kind: Service
  metadata:
    name: my-flask-app-service
  spec:
    selector:
      app: flask-app
    type: LoadBalancer
    ports:
      - name: flask-app
        port: 5455
        targetPort: 5455
