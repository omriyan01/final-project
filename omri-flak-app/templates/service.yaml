apiVersion: v1
kind: Service
metadata:
  name: omri-flask-app-service
spec:
  selector:
      app: flask-app
  type: LoadBalancer
  ports:
    - name: omri-flask-app
      port: 6055 # Port exposed by your Flask application
      targetPort: 6055 # Port your Flask application is listening on

