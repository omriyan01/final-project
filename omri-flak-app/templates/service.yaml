apiVersion: v1
kind: Service
metadata:
  name: omri-flask-app-service
spec:
  selector:
      app: omri-flask-app # Match the label used in Deployment
  type: LoadBalancer
  ports:
    - name: omri-flask-app
      port: 9001 # Port exposed by your Flask application
      targetPort: 9001


